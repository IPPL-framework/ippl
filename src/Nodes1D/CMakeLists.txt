# -----------------------------------------------------------------------------
# src/Nodes1D/CMakeLists.txt
#
# Header-only 1D node/weight finders (Gauss–Jacobi / Legendre / Lobatto).
# Public headers are visible via target_include_directories(ippl ...) and installed
# centrally by InstallIppl.cmake.
# -----------------------------------------------------------------------------
