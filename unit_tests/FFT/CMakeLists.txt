file(RELATIVE_PATH _relPath "${PROJECT_SOURCE_DIR}" "${CMAKE_CURRENT_SOURCE_DIR}")
message(STATUS "Adding unit tests found in ${_relPath}")

add_ippl_test(FFT)
if(IPPL_ENABLE_FINUFFT)
  set(_nufft_link_libs finufft)
  if(TARGET cufinufft)
    list(APPEND _nufft_link_libs cufinufft)
  endif()
  add_ippl_test(NUFFT TIMEOUT 300 LINK_LIBS ${_nufft_link_libs})
  add_ippl_test(NUFFTAccuracy TIMEOUT 1800 LINK_LIBS ${_nufft_link_libs})
else()
  add_ippl_test(NUFFT TIMEOUT 300)
  add_ippl_test(NUFFTAccuracy TIMEOUT 1800)
endif()
